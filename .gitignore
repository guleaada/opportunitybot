# Environment & secrets
.env
.env.local
credentials.json
token.json
calendar_token.pickle
*.pickle

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.pytest_cache/

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store

# Temporary
# URL cache — huge & regeneratable, do NOT commit (no inline comments in .gitignore!)
data/url_cache/
*.log

# Generated cover-letter drafts (personal, regeneratable)
drafts/

# ────────────────────────────────────────────────────────────────
# DO NOT IGNORE the persistence files below — they MUST be committed
# so the bot stays stateful across ephemeral GitHub Actions runs:
#   data/seen_opportunities.json
#   data/application_tracker.json
#   data/watchlist.json
#   data/cost_log.json
#   data/activity_log.json
# (They are intentionally NOT listed here.)
# ────────────────────────────────────────────────────────────────
